epochs: 20
batch_size: 256
num_workers: 4
pin_memory: true

accelerator: auto
devices: auto
precision: 16-mixed

monitor: val_acc
monitor_mode: max
log_every_n_steps: 50
progress_bar_refresh_rate: 10
