epochs: 20
batch_size: 256
num_workers: 4
pin_memory: true
use_all_sigmas: true
apply_norm_if_meta: true

accelerator: auto
devices: auto
precision: 16-mixed

loss_type: l1
monitor: val_loss
monitor_mode: min
log_every_n_steps: 50
progress_bar_refresh_rate: 10
